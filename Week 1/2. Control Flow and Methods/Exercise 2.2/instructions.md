# Exercise 2.2 — FizzBuzz

Create `FizzBuzz.java`:

```java
    public static String fizzBuzzValue(int n) {
    // Returns "FizzBuzz", "Fizz", "Buzz", or the number as a String
}
```

1. Then in main, loop 1–100 and print each result.
2. Separate the logic (the method) from the output (the loop)
3. This is already separation of concerns
